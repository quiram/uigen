Check for vulnerabilities in Node packages and automatically fix trivial ones.

Run `npm audit` to check for vulnerabilities, then run `npm audit fix` to automatically apply safe fixes. After that, run `npm test` to verify that all tests still pass. Report the results of each step clearly.
